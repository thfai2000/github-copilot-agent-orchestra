# Infrastructure values — shared Redis + namespace
namespace: agent-orchestra

redis:
  image: redis:7-alpine
  replicas: 1
  resources:
    requests:
      memory: 64Mi
      cpu: 100m
    limits:
      memory: 128Mi
      cpu: 250m
